name: cubit_study
description: Flutter Study Group - Cubit Intro

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #Keep this section alphabetized
  cupertino_icons: ^0.1.3
  equatable: ^1.2.4
  flutter_bloc: ^6.0.5
  logging: ^0.11.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  #Keep this section alphabetized
  effective_dart: ^1.2.4

flutter:
  uses-material-design: true
